
describe('svg-text-in-shape, e2e tests - ', () => {

    beforeEach(() => {
        await page.goto(__dirname);
    });

    test('Text that does not fit in the shape is appended with ...', () => {

    })
})